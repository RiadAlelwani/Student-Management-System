/**
 * This is the module declaration for the Student Management System (SMS) project.
 * It specifies which modules this module requires in order to compile and run.
 */
module SMS {
	// Requires the java.sql module to enable JDBC (Java Database Connectivity) features
	requires java.sql;
	
	// Requires the java.desktop module to enable GUI features (like Swing)
	requires java.desktop;
}
